<%= form_for(raceadmin) do |f| %>
  <% if raceadmin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(raceadmin.errors.count, "error") %> prohibited this raceadmin from being saved:</h2>

      <ul>
      <% raceadmin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %>
    <%= f.text_field :year %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
